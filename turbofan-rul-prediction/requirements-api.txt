# Minimal deps to run the FastAPI inference service
# Note: model inference uses PyTorch.

fastapi
uvicorn

# Prefer CPU wheels for Linux containers (smaller than CUDA builds)
--extra-index-url https://download.pytorch.org/whl/cpu
torch
numpy
