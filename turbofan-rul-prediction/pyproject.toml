[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.black]
line-length = 100

[tool.isort]
profile = "black"

[tool.coverage.run]
source = ["src"]
omit = [
	"src/data/test_data.py",
	"src/dl/optimize_lstm.py",
	"src/dl/train_lstm.py",
]

[tool.coverage.report]
show_missing = true